# Run from this directory every five minutes. Replace /absolute/path before installing.
*/5 * * * * cd /absolute/path/automation-demo && mkdir -p monitoring && /usr/bin/python3 monitor.py >> monitoring/cron.log 2>&1
